import { beforeAll, afterAll, beforeEach, afterEach } from "vitest";

beforeAll(async () => {
  process.env.NODE_ENV = "test";
});

afterAll(async () => {
});

beforeEach(async () => {
});

afterEach(async () => {
});
